

const a = 4; // 
const b = 6; // 

/*Bitwise AND (&) */
console.log(a & b);
/*Bitwise OR (|) */
console.log(a | b);
/*Bitwise XOR (^) */
console.log(a ^ b); 

/*Bitwise AND assignment (&=)*/
let c = 5;
c &= 3;

/*Bitwise OR assignment (|=) */
let d=4;
d |= 3

/*Bitwise XOR assignment (^=) */
let e=4;
e ^= 3;
/*Bitwise NOT (~) */
const f=1
console.log(~f); 







