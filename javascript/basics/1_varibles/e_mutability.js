/*We can change the data type and the value of  let and var varibles.*/
/*We can not modify the varibles declayred with const */

//We can reassign the values in java script
var myKey1=true;
    myKey1=2;
    myKey1="test";
    console.log(myKey1);
//This is valid in Java script

let myKey2=true;
    myKey2=2;
    myKey2="test";
    console.log(myKey2);
//This is valid in Java script

/*If we define the value with const keyword then we can not change value or data type */
    // const z=2;
    // z=4;
    // console.log(z)