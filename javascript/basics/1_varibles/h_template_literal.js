

//Template Literals use back-ticks (``) rather than the quotes ("") to define a string.
let text = `Hello World!`;

console.log(text)

//Template literal and Interploation
//We can use varibles inside the strings called interpolation
///Syntax: `  ${}`
    var  myName="Subbu";
    var greet='Hello' +myName;
    var greet2= `Hello ${myName} !!! How are you`;
    console.log(greet);
    console.log(greet2)

    