let mul = (a, b) => {  
    // Put curly brace opens a multiline function.
    let result = a + b;
    return result; // if we use curly braces, then we need to "return"
  };
  
console.log( mul(1, 2) ); // 3
